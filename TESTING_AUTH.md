# 🧪 Тесты авторизации - Быстрый старт

## ✅ Что сделано

Созданы тесты для всех endpoints авторизации:

- ✅ **POST /api/auth/register** - 8 тестов
- ✅ **POST /api/auth/login** - 6 тестов  
- ✅ **GET /api/auth/me** - 4 теста
- ✅ **PUT /api/auth/profile** - 5 тестов

**Всего: 23 теста** 🎉

## 🚀 Как запустить

```bash
# 1. Перейти в папку server
cd server

# 2. Установить зависимости (если еще не установлены)
npm install

# 3. Запустить тесты
npm test

# Или только тесты авторизации
npm run test:auth

# Или в режиме watch (автоматический перезапуск при изменениях)
npm run test:watch
```

## 📋 Что тестируется

### Регистрация (POST /api/auth/register)
- ✅ Успешная регистрация
- ✅ Ошибка при существующем email
- ✅ Валидация email, пароля, имени, userType
- ✅ Хеширование пароля
- ✅ Опциональные поля (phone, age)

### Вход (POST /api/auth/login)
- ✅ Успешный вход
- ✅ Ошибка при несуществующем пользователе
- ✅ Ошибка при неверном пароле
- ✅ Валидация данных
- ✅ Генерация JWT токена

### Получение профиля (GET /api/auth/me)
- ✅ Успешное получение данных
- ✅ Ошибка при отсутствии токена
- ✅ Ошибка при невалидном токене
- ✅ Ошибка при несуществующем пользователе

### Обновление профиля (PUT /api/auth/profile)
- ✅ Успешное обновление
- ✅ Валидация полей
- ✅ Ошибка при отсутствии токена

## 📁 Структура файлов

```
server/
├── src/
│   ├── __tests__/
│   │   ├── setup.js          # Настройка тестового окружения
│   │   ├── auth.test.js      # Тесты авторизации (23 теста)
│   │   └── README.md         # Подробная документация
│   └── routes/
│       └── auth.js           # Тестируемый код
├── jest.config.js            # Конфигурация Jest
└── package.json             # Скрипты: test, test:auth, test:watch
```

## 🔧 Технологии

- **Jest** - фреймворк для тестирования
- **Supertest** - для тестирования HTTP endpoints
- **Моки Prisma** - тесты не требуют реальной БД

## 💡 Примечания

- Тесты используют моки, поэтому **не требуют базы данных**
- Все тесты изолированы друг от друга
- Каждый тест очищает моки перед выполнением
- Тесты проверяют как успешные сценарии, так и ошибки

## 📊 Пример вывода

После запуска `npm test` вы увидите:

```
PASS  src/__tests__/auth.test.js
  Auth API Tests
    POST /api/auth/register
      ✓ should register a new user successfully
      ✓ should return 400 if user already exists
      ...
    POST /api/auth/login
      ✓ should login user with valid credentials
      ...
    GET /api/auth/me
      ✓ should return user data with valid token
      ...
    PUT /api/auth/profile
      ✓ should update user profile with valid data
      ...

Test Suites: 1 passed, 1 total
Tests:       23 passed, 23 total
```

## 🎯 Следующие шаги

После того как тесты авторизации работают, можно добавить:

1. Тесты для центров (`/api/centers`)
2. Тесты для статей (`/api/articles`)
3. Тесты для бронирований (`/api/bookings`)

Но для MVP этого достаточно! 🚀


