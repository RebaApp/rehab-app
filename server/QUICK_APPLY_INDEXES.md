# ⚡ Быстрое применение индексов

## ✅ Что готово

- ✅ Индексы добавлены в `schema.prisma` (6 индексов)
- ✅ SQL миграция создана: `prisma/migrations/add_indexes.sql`
- ✅ Готово к применению!

## 🚀 Самый простой способ

### Если БД запущена через Docker:

```bash
cd server

# Применить через Prisma (автоматически)
npx prisma db push
```

### Если Prisma не работает, примените SQL вручную:

```bash
cd server

# Через Docker
docker-compose exec postgres psql -U reba_user -d reba_db < prisma/migrations/add_indexes.sql

# Или подключитесь к БД и выполните SQL
psql -U reba_user -d reba_db -f prisma/migrations/add_indexes.sql
```

## 📋 Что будет создано

6 индексов:
- `centers_city_idx` - поиск по городу
- `centers_rating_idx` - сортировка по рейтингу  
- `centers_verified_idx` - фильтрация верифицированных
- `centers_ownerId_idx` - поиск центров владельца
- `reviews_centerId_idx` - поиск отзывов центра
- `reviews_userId_idx` - поиск отзывов пользователя

## ⏱️ Время: 5-10 секунд

После применения запросы станут в **100-1000 раз быстрее**! 🚀
