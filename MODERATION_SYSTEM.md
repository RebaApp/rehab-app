# 🛡️ Система модерации центров РЕБА

## 📋 Текущая реализация

### Статусы модерации:
- **PENDING** - Центр отправлен на модерацию
- **APPROVED** - Центр одобрен, можно оплачивать подписку
- **REJECTED** - Центр отклонен (с комментарием)

### Процесс:
1. Владелец создает центр → статус `PENDING`
2. Модератор/ИИ проверяет → устанавливает `APPROVED` или `REJECTED`
3. Если одобрен → владелец оплачивает подписку
4. После оплаты → центр появляется в поиске

---

## 🎯 Рекомендуемое решение: Гибридный подход

### Этап 1: Админ-панель (сейчас)
**Веб-интерфейс для модераторов**

#### Структура:
```
/admin/moderation
├── /pending - Центры на модерации
├── /approved - Одобренные центры
├── /rejected - Отклоненные центры
└── /center/:id - Детальная страница центра
```

#### Функционал:
- ✅ Просмотр всех данных центра
- ✅ Просмотр документов (лицензия, сертификаты)
- ✅ Кнопки: "Одобрить" / "Отклонить"
- ✅ Поле для комментария модератора
- ✅ Фильтры и поиск
- ✅ История изменений

#### Технологии:
- **Frontend**: React + TypeScript (или простой HTML)
- **Backend**: Использовать существующие API endpoints
- **Auth**: JWT с ролью `ADMIN`

---

### Этап 2: ИИ-помощник (позже)
**Автоматическая первичная проверка**

#### Что проверяет ИИ:
1. **Заполненность данных:**
   - Все обязательные поля заполнены
   - Форматы корректны (телефон, email, координаты)

2. **Документы:**
   - Лицензия присутствует и читаема
   - Сертификаты валидны
   - OCR для извлечения данных из документов

3. **Контент:**
   - Описание не содержит запрещенных слов
   - Фото соответствуют центру
   - Нет дубликатов

#### API для ИИ:
```javascript
POST /api/admin/ai-check/:centerId
// Возвращает:
{
  "status": "auto_approved" | "needs_review" | "auto_rejected",
  "confidence": 0.95,
  "issues": ["Лицензия нечеткая", "Отсутствует сертификат"],
  "suggestions": ["Запросить более четкое фото лицензии"]
}
```

#### Интеграция:
- **OpenAI GPT-4 Vision** - для анализа документов
- **Claude Vision** - альтернатива
- **Custom OCR** - для извлечения текста из документов

---

## 🚀 План реализации

### Фаза 1: Админ-панель (1-2 недели)

#### Backend API:
```javascript
// GET /api/admin/centers/pending - Список на модерации
// GET /api/admin/centers/:id - Детали центра
// PUT /api/admin/centers/:id/approve - Одобрить
// PUT /api/admin/centers/:id/reject - Отклонить
```

#### Frontend:
- Простой React-интерфейс
- Таблица центров
- Модальное окно для деталей
- Кнопки действий

### Фаза 2: ИИ-интеграция (2-3 недели)

#### Задачи:
1. Настроить OpenAI/Claude API
2. Создать промпты для проверки
3. Интегрировать OCR для документов
4. Добавить автоматическую проверку при создании центра

#### Логика:
```javascript
// При создании центра:
1. ИИ проверяет → возвращает результат
2. Если confidence > 0.9 → auto_approved
3. Если confidence < 0.7 → auto_rejected
4. Иначе → needs_review (ручная модерация)
```

---

## 📊 Структура данных

### Центр на модерации:
```typescript
{
  id: string;
  name: string;
  // ... все данные центра
  moderationStatus: 'PENDING' | 'APPROVED' | 'REJECTED';
  moderationComment?: string;
  moderatedBy?: string; // ID модератора
  moderatedAt?: Date;
  aiCheckResult?: {
    status: string;
    confidence: number;
    issues: string[];
  };
}
```

---

## 🔐 Безопасность

### Роли:
- **ADMIN** - полный доступ к модерации
- **MODERATOR** - может одобрять/отклонять, но не удалять
- **USER** - только просмотр своих центров

### Middleware:
```javascript
// server/src/middleware/auth.js
const adminMiddleware = async (req, res, next) => {
  if (req.user.userType !== 'ADMIN') {
    return res.status(403).json({ error: 'Admin access required' });
  }
  next();
};
```

---

## 📱 UI/UX для модератора

### Главная страница:
```
┌─────────────────────────────────────┐
│  Модерация центров                  │
├─────────────────────────────────────┤
│  [PENDING: 5] [APPROVED: 12]        │
│                                     │
│  🔍 Поиск...                        │
│                                     │
│  ┌───────────────────────────────┐  │
│  │ Центр "Реабилитация+"         │  │
│  │ Москва • Создан 2 дня назад   │  │
│  │ [Просмотреть] [Одобрить]     │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

### Детальная страница:
```
┌─────────────────────────────────────┐
│  ← Назад                            │
│                                     │
│  Центр: Реабилитация+              │
│  Город: Москва                      │
│  Владелец: Иван Иванов              │
│                                     │
│  📄 Лицензия: [Просмотреть]        │
│  📄 Сертификаты: [Просмотреть]     │
│                                     │
│  Описание: ...                      │
│                                     │
│  [✅ Одобрить] [❌ Отклонить]       │
│  Комментарий: [________________]    │
└─────────────────────────────────────┘
```

---

## 🎯 Приоритеты

1. **Сейчас**: Создать простую админ-панель для ручной модерации
2. **Через месяц**: Добавить ИИ для первичной проверки
3. **Позже**: Автоматизация и улучшение ИИ-проверки

---

## 💡 Альтернативные решения

### Вариант A: Использовать готовые сервисы
- **Moderation API** от различных провайдеров
- Плюсы: быстро, не нужно разрабатывать
- Минусы: дорого, меньше контроля

### Вариант B: Аутсорсинг модерации
- Нанять модераторов на фрилансе
- Плюсы: качественная проверка
- Минусы: дорого, медленно

### Вариант C: Сообщество модераторов
- Привлечь опытных пользователей
- Плюсы: бесплатно, экспертиза
- Минусы: нужна система мотивации

---

## ✅ Следующие шаги

1. **Исправить ошибку создания центра** (применить миграцию БД)
2. **Создать API endpoints для модерации**
3. **Создать простую админ-панель**
4. **Протестировать процесс модерации**
5. **Добавить ИИ-проверку (опционально)**

