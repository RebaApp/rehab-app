# 🔐 Миграция секретов в переменные окружения

## ✅ Что сделано

Все секреты перемещены из исходного кода в переменные окружения для безопасности.

### Измененные файлы:

1. **`src/services/authService.ts`**
   - ✅ Yandex OAuth Client ID и Secret теперь берутся из `process.env`

2. **`src/config/firebase.ts`**
   - ✅ Все Firebase конфигурационные данные теперь берутся из `process.env`

3. **`.env.example`** (новый файл)
   - ✅ Шаблон для переменных окружения

## 🚀 Как настроить

### Шаг 1: Создайте .env файл

```bash
# В корне проекта
cp .env.example .env
```

### Шаг 2: Заполните реальными значениями

Откройте `.env` и заполните значения. **Ниже текущие значения для быстрой миграции:**

```env
# ============================================
# Yandex OAuth (текущие значения)
# ============================================
EXPO_PUBLIC_YANDEX_CLIENT_ID=b08282bbc8e8435d88e7c02b2098496f
EXPO_PUBLIC_YANDEX_CLIENT_SECRET=ba5c7710a1fa4cd58ecccbacc514c890

# ============================================
# Firebase (текущие значения)
# ============================================
EXPO_PUBLIC_FIREBASE_API_KEY=AIzaSyC8w3EGQXPBdmFUl6J8zbuvpS0GQu9jv0M
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=reba-df34c.firebaseapp.com
EXPO_PUBLIC_FIREBASE_PROJECT_ID=reba-df34c
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=reba-df34c.firebasestorage.app
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=840686619600
EXPO_PUBLIC_FIREBASE_APP_ID=1:840686619600:web:f43324594e3a579a2e87d5
EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID=G-ZM80BLM06G

# ============================================
# API Configuration
# ============================================
EXPO_PUBLIC_API_URL=http://localhost:3001/api
```

### Шаг 3: Перезапустите Expo

После создания/изменения `.env` файла нужно перезапустить Expo с очисткой кэша:

```bash
expo start -c
```

## ⚠️ Важно!

1. **НИКОГДА не коммитьте `.env` файл в git!**
   - ✅ Файл уже добавлен в `.gitignore`
   - Проверьте, что `.env` не попал в репозиторий: `git status`

2. **Используйте `.env.example` как шаблон**
   - ✅ Коммитьте только `.env.example` (без реальных значений)
   - Каждый разработчик создает свой `.env` из шаблона

3. **Для продакшена:**
   - Используйте переменные окружения на сервере
   - Не храните секреты в коде приложения
   - Используйте секретные менеджеры (AWS Secrets Manager, HashiCorp Vault и т.д.)

## 🔍 Проверка

После настройки проверьте:

1. ✅ Приложение запускается без ошибок
2. ✅ Yandex OAuth работает (проверьте вход через Яндекс)
3. ✅ Firebase подключение работает (проверьте авторизацию)
4. ✅ В консоли нет предупреждений о недостающих переменных

## 📚 Дополнительная информация

- [Expo Environment Variables](https://docs.expo.dev/guides/environment-variables/)
- [Firebase Setup](https://firebase.google.com/docs/web/setup)
- [Yandex OAuth](https://yandex.ru/dev/id/doc/ru/)

## 🔄 Что изменилось в коде

### До:
```typescript
// ❌ Секреты в коде
private yandexClientId = 'b08282bbc8e8435d88e7c02b2098496f';
private yandexClientSecret = 'ba5c7710a1fa4cd58ecccbacc514c890';
```

### После:
```typescript
// ✅ Секреты из переменных окружения
this.yandexClientId = process.env.EXPO_PUBLIC_YANDEX_CLIENT_ID || '';
this.yandexClientSecret = process.env.EXPO_PUBLIC_YANDEX_CLIENT_SECRET || '';
```

---

**Готово!** Теперь ваши секреты в безопасности! 🔒
