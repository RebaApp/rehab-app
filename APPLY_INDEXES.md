# 🚀 Как применить индексы в БД

## ✅ Индексы уже добавлены в schema.prisma

Добавлено **6 индексов**:
- Center: city, rating, verified, ownerId (4 индекса)
- Review: centerId, userId (2 индекса)

## 📋 Следующий шаг: Применить миграцию

### Вариант 1: Если БД уже существует (рекомендуется)

```bash
cd server

# Создать и применить миграцию
npx prisma migrate dev --name add_database_indexes
```

Это:
1. Создаст файл миграции
2. Применит изменения к БД
3. Обновит Prisma Client

### Вариант 2: Если БД пустая или тестовая

```bash
cd server

# Просто применить схему (без создания миграции)
npx prisma db push
```

### Вариант 3: Только создать миграцию (без применения)

```bash
cd server

# Создать миграцию, но не применять
npx prisma migrate dev --create-only --name add_database_indexes
```

## ⏱️ Время выполнения

- При 100 центрах: 2-5 секунд
- При 1,000 центрах: 5-10 секунд
- При 10,000 центрах: 30-60 секунд

## ✅ После применения

Проверьте, что всё работает:

```bash
# Запустить сервер
npm start

# Проверить API
curl http://localhost:3001/api/centers?city=Москва
```

Запросы должны быть **значительно быстрее**! 🚀
