# 🤝 Руководство по вкладу в проект РЕБА

Спасибо за интерес к проекту РЕБА! Мы приветствуем любой вклад в развитие платформы.

## 🚀 Быстрый старт

1. **Fork** репозитория
2. **Clone** вашу копию: `git clone https://github.com/YOUR_USERNAME/rehab-app.git`
3. **Создайте ветку**: `git checkout -b feature/amazing-feature`
4. **Внесите изменения** и закоммитьте: `git commit -m 'Add amazing feature'`
5. **Push** в вашу ветку: `git push origin feature/amazing-feature`
6. **Создайте Pull Request**

## 📋 Типы вкладов

### 🐛 Исправление ошибок
- Создайте Issue с описанием проблемы
- Используйте шаблон "Bug Report"
- Приложите скриншоты и логи

### ✨ Новые функции
- Обсудите идею в Discussions
- Создайте Issue с описанием функции
- Следуйте архитектуре проекта

### 📚 Документация
- Улучшение README
- Добавление комментариев в код
- Создание руководств пользователя

### 🧪 Тестирование

### Frontend тесты
- **Unit тесты** - тестирование компонентов и утилит
- **Integration тесты** - тестирование взаимодействия компонентов
- **E2E тестирование** - тестирование пользовательских сценариев

### Backend тесты
- **Unit тесты** - тестирование отдельных функций
- **Integration тесты** - тестирование API endpoints с Supertest
- **Auth тесты** - тестирование аутентификации и авторизации
- **Тестирование производительности** - проверка времени ответа API

### Примеры тестов

#### Backend API тест
```javascript
// server/src/__tests__/auth.test.js
describe('POST /api/auth/register', () => {
  it('should register a new user', async () => {
    const response = await request(app)
      .post('/api/auth/register')
      .send({
        email: 'test@example.com',
        password: 'password123',
        name: 'Test User'
      });
    
    expect(response.status).toBe(201);
    expect(response.body).toHaveProperty('token');
  });
});
```

#### Frontend компонент тест
```typescript
// src/__tests__/components/AuthModal.test.tsx
import { render, fireEvent } from '@testing-library/react-native';
import AuthModal from '../components/auth/AuthModal';

test('should call onLogin when login button is pressed', () => {
  const onLogin = jest.fn();
  const { getByText } = render(<AuthModal onLogin={onLogin} />);
  
  fireEvent.press(getByText('Войти'));
  expect(onLogin).toHaveBeenCalled();
});
```

## 🎯 Стандарты кода

### TypeScript (Frontend)
```typescript
// ✅ Хорошо
interface User {
  id: string;
  email: string;
  name: string;
  userType: 'USER' | 'CENTER' | 'ADMIN';
}

// ❌ Плохо
const user = { id: 1, email: "test", name: "test" };
```

### JavaScript (Backend)
```javascript
// ✅ Хорошо - используйте Prisma singleton
const prisma = require('../utils/prisma');

// ❌ Плохо - не создавайте новый экземпляр
const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();
```

### React Native
```typescript
// ✅ Хорошо
const ProfileScreen: React.FC<ProfileScreenProps> = memo(({ user }) => {
  return <View style={styles.container}>...</View>;
});

// ❌ Плохо
function ProfileScreen(user) {
  return <div>...</div>;
}
```

### Стили (React Native)
```typescript
// ✅ Хорошо
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FFFFFF',
    paddingHorizontal: responsivePadding(16),
  },
});

// ❌ Плохо
const styles = {
  container: { flex: 1, backgroundColor: 'white', padding: 16 },
};
```

### Безопасность (Backend)
```javascript
// ✅ Хорошо - валидация и санитизация
router.post('/register', [
  body('email').isEmail().normalizeEmail(),
  body('password').isLength({ min: 6 }),
], async (req, res) => {
  const errors = validationResult(req);
  if (!errors.isEmpty()) {
    return res.status(400).json({ errors: errors.array() });
  }
  // ...
});

// ❌ Плохо - нет валидации
router.post('/register', async (req, res) => {
  const { email, password } = req.body;
  // Прямое использование без проверки
});
```

### Работа с базой данных
```javascript
// ✅ Хорошо - используйте индексы и оптимизированные запросы
const centers = await prisma.center.findMany({
  where: { city: 'Москва' }, // Использует индекс на city
  include: { photos: true },
  take: 20,
  orderBy: { rating: 'desc' } // Использует индекс на rating
});

// ❌ Плохо - запрос без оптимизации
const centers = await prisma.center.findMany({
  where: { 
    name: { contains: 'Москва' } // Нет индекса на name
  }
});
```

## 🔍 Процесс ревью

1. **Автоматические проверки** - CI/CD pipeline
2. **Code review** - минимум 2 аппрува
3. **Тестирование** - все тесты должны проходить
4. **Документация** - обновление при необходимости
5. **Проверка безопасности** - валидация входных данных, защита от инъекций
6. **Проверка производительности** - использование индексов БД, кэширование где необходимо

## 📝 Коммиты

Используйте конвенцию [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: добавить авторизацию через Яндекс
fix: исправить ошибку в ProfileScreen
docs: обновить README
style: форматирование кода
refactor: рефакторинг store
test: добавить тесты для authService
perf: добавить Redis кэширование для центров
security: исправить уязвимость в валидации паролей
```

### Типы коммитов:
- `feat` - новая функциональность
- `fix` - исправление бага
- `docs` - изменения в документации
- `style` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor` - рефакторинг кода
- `perf` - улучшения производительности
- `test` - добавление или исправление тестов
- `security` - исправления безопасности
- `chore` - обновление зависимостей, конфигурации и т.д.

## 🏷️ Создание Issues

### Шаблон для багов
```markdown
## 🐛 Описание ошибки
Краткое описание проблемы

## 🔄 Шаги воспроизведения
1. Перейти в '...'
2. Нажать на '...'
3. Увидеть ошибку

## 📱 Окружение
- OS: iOS/Android
- Версия: 0.72
- Устройство: iPhone/Android

## 📸 Скриншоты
Приложите скриншоты если возможно
```

### Шаблон для функций
```markdown
## ✨ Описание функции
Краткое описание новой функции

## 🎯 Проблема
Какую проблему решает эта функция?

## 💡 Предлагаемое решение
Как вы предлагаете реализовать?

## 🔄 Альтернативы
Какие альтернативы рассматривались?
```

## 🎉 Признание

Все участники будут упомянуты в:
- README файле
- Release notes
- Специальной странице благодарностей

## 🔧 Технические детали

### Работа с базой данных
- **Всегда используйте Prisma singleton** из `server/src/utils/prisma.js`
- **Используйте индексы** для часто запрашиваемых полей (city, rating, verified)
- **Применяйте миграции** перед коммитом изменений схемы
- **Тестируйте запросы** на производительность

### Кэширование Redis
- **Кэшируйте часто запрашиваемые данные** (списки центров, статьи)
- **Устанавливайте разумный TTL** (5-15 минут для динамических данных)
- **Инвалидируйте кэш** при обновлении данных
- **Используйте осмысленные ключи** (`centers:city:moscow:page:1`)

### Безопасность
- **Всегда валидируйте входные данные** через express-validator
- **Используйте bcrypt** для хеширования паролей (12 раундов)
- **Защищайте маршруты** через auth middleware
- **Проверяйте права доступа** для административных операций
- **Не логируйте чувствительные данные** (пароли, токены)

### Производительность
- **Используйте connection pooling** (уже настроено в Prisma)
- **Оптимизируйте запросы** - избегайте N+1 проблем
- **Кэшируйте результаты** сложных запросов
- **Используйте пагинацию** для больших списков

## 📚 Полезные ресурсы

- [Prisma Documentation](https://www.prisma.io/docs)
- [Express Best Practices](https://expressjs.com/en/advanced/best-practice-security.html)
- [React Native Testing Library](https://callstack.github.io/react-native-testing-library/)
- [Redis Caching Patterns](https://redis.io/docs/manual/patterns/)

## ❓ Вопросы?

Если у вас есть вопросы, создайте Discussion или напишите в Telegram: @vadimsao

---

**Спасибо за ваш вклад в РЕБА! 🚀**
